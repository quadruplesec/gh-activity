/*
TODO

package github

import "fmt"

type ActivityFeed []Event

func formatAggregatedPushes(count int, repo string) string {
	if count == 0 {
		return ""
	}
	word := "commits"
	if count == 1 {
		word == "commit"
	}

	action := Colorize(GetEventColor("PushEvent"), fmt.)
}
*/